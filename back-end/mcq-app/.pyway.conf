database_type: postgres

# Connection details (prefer overriding via env vars in real deployments)
database_username: postgres
database_password: postgres
database_host: localhost
database_port: 5432
database_name: mcq_db

# Migration location (relative to this file)
database_migration_dir: sql/migrations

# Schema history table (Flyway-like)
database_table: mcq.pyway_schema_history

